@model Core_OdeToCode.ViewModels.HomePageViewModel

@{ 
    ViewBag.Title = "Home";
}

<h1>Welcome</h1>
@foreach(var restaurant in Model.Restaurants)
{
    @Html.Partial("_Summary", restaurant);
}

@*<table border="1" style="border-collapse:collapse;">
    @foreach (var restaurant in Model.Restaurants)
    {
        <tr>
            <td style="padding:3px">@Html.ActionLink(restaurant.Id.ToString(), "Details", new { id = restaurant.Id })</td>
            <td style="padding:3px">@restaurant.Name</td>
            <td style="padding:3px">@restaurant.Cuisine</td>
            <td>
                <a asp-action="Details" asp-route-id="@restaurant.Id">Details</a>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@restaurant.Id">Edit</a>
            </td>
        </tr>
    }
</table>*@
<br />
<a style="border:1px solid black;" asp-controller="Home" asp-action="Create">Create New</a>
@section pageSection{
    <i>This is from Home and <b>@Model.CurrentGreeting</b></i>
}